# SV-Trace 架构文档

> 📅 更新时间: 2026-05-02  
> 🎯 版本: v1.0

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Parser Foundation v1.0                          │
│                  src/parse/ (302 个原子解析器)                     │
│         603 语法 · 112% 覆盖率 · AST遍历无正则                     │
└─────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │
                    ┌───────────────┴───────────────┐
                    │      SVParser (主入口)        │
                    │    parse/parser.py           │
                    └───────────────┬───────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐         ┌───────────────┐         ┌───────────────┐
│   trace/     │         │   debug/     │         │   verify/    │
│  27 files    │         │  14 files    │         │  12 files    │
│ 17 依赖parse │         │  1 依赖parse │         │  1 依赖parse │
├───────────────┤         ├───────────────┤         ├───────────────┤
│ 信号追踪     │         │ 高级分析     │         │ 验证支持     │
│ · driver     │         │ · constraint │         │ · 约束生成   │
│ · load       │         │ · fsm       │         │ · 覆盖率    │
│ · dataflow   │         │ · cdc       │         │ · 种子管理   │
│ · connection │         │ · reset     │         │             │
│ · controlflow│         │ · class    │         │             │
│ · datapath   │         │             │         │             │
└───────────────┘         └───────────────┘         └───────────────┘
        │
        ├───────────────────────────────┬───────────────────────────────┐
        │                               │                               │
        ▼                               ▼                               ▼
┌───────────────┐         ┌───────────────┐         ┌───────────────┐
│   query/     │         │   lint/       │         │   dft/       │
│  11 files    │         │  4 files      │         │  4 files     │
│  1 依赖parse │         │  3 依赖parse  │         │  1 依赖parse │
├───────────────┤         ├───────────────┤         ├───────────────┤
│ 信号查询     │         │ 代码检查     │         │ DFT分析      │
│ · signal     │         │ · linter     │         │ · 覆盖率    │
│ · path       │         │ · code_quality│         │ · 复位域   │
│ · condition  │         │ · syntax_check│         │ · MBIST     │
└───────────────┘         └───────────────┘         └───────────────┘
```

---

## 模块统计

| 模块 | 文件数 | 依赖Parse | 主要功能 |
|------|--------|-----------|----------|
| **parse** | 302 | 自身 | AST 解析基座 |
| **trace** | 27 | 17 (63%) | 信号追踪 (驱动/负载/依赖/数据流) |
| **debug** | 14 | 1 (~7%) | 高级分析 (FSM/CDC/约束) |
| **verify** | 12 | 1 (~8%) | 验证支持 (约束生成/覆盖率) |
| **query** | 11 | 1 (~9%) | 查询接口 |
| **lint** | 4 | 3 (75%) | 代码检查 |
| **dft** | 4 | 1 (25%) | DFT 分析 |
| **core** | 1 | 0 | 数据模型 |
| **power** | 2 | 0 | 功耗分析 |
| **apps** | 4 | 3 | CLI 应用入口 |

---

## Parser Foundation v1.0

### 设计原则

1. **AST 优先** - 所有解析器使用 pyslang AST 遍历，无正则表达式
2. **原子化** - 每个解析器专注单一语法类别
3. **可组合** - 基于现有 parser 可派生新 parser

### 核心指标

| 指标 | 值 |
|------|-----|
| 解析器文件 | 302 个 |
| 支持语法 | 603 种 |
| pyslang 总语法 | 536 种 |
| 覆盖率 | 112% |
| sv-tests 成功率 | 100% |

详细文档: [PARSER_SUPPORT.md](./PARSER_SUPPORT.md)

---

## 数据流

```
代码 (.sv)
    │
    ▼
┌─────────────┐
│   SVParser  │ ◄─── parse/parser.py
└─────────────┘
    │
    ├──► trace/     ──► 驱动/负载/依赖/数据流分析
    │
    ├──► debug/     ──► FSM/CDC/约束分析
    │
    ├──► verify/    ──► 验证计划/覆盖率
    │
    ├──► query/     ──► 信号/路径查询
    │
    ├──► lint/      ──► 代码质量检查
    │
    └──► dft/        ──► DFT 分析
```

---

## 依赖关系说明

### 高依赖 (trace/)

`trace/` 模块直接操作 AST 进行信号追踪，63% 的文件直接依赖 parse：

- `driver.py` - 驱动追踪
- `load.py` - 负载追踪
- `dataflow.py` - 数据流分析
- `connection.py` - 连接追踪
- `controlflow.py` - 控制流分析

### 中依赖 (lint/)

`lint/` 模块需要源码进行模式匹配，75% 依赖 parse：

- `linter.py` - 代码风格检查
- `code_quality.py` - 代码质量评分
- `syntax_check.py` - 语法检查

### 低依赖 (debug/verify/query/dft)

这些模块有自己的封装或使用其他方式获取信息：

- `constraint_parser_v2.py` - 使用 SVParser 封装
- 其他模块通过 trace/ 的结果进行二次分析

---

*此文档由 sv-trace 自动生成*
